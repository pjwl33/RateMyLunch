<%= form_for @meal do |f| %>

  <%= f.label :comment %>
  <%= f.text_area :comment %>

  <%= f.label :ingredients %>
  <%= f.text_area :ingredients %>

  <%= f.label :goals %>
  <%= f.select :goal_id, @goals.collect {|goal| [goal.name]} %>

  <%= f.submit %>

<% end %>